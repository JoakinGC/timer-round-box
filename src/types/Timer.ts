export type TimerControl = {
    numberComplets:number,
    numberRounds:number,
    timeBreaks:number,
    timeRound:number,
}


export type TimerProps = {
    hours:number,
    minutes:number,
    seconds:number,
}