export type TimerControl = {
    numberComplets:number,
    numberRounds:number,
    timeBreaks:number,
    timeRound:number,
    isBreak:boolean,
}


export type TimerProps = {
    hours:number,
    minutes:number,
    seconds:number,
}